heroku:
  stage: deploy
  only:
    - main
  before_script:
    - apk update && apk add git
    - git --version
  script:
    - git branch -r
  environment:
    name: production
    url: https://reservenow.herokuapp.com/
